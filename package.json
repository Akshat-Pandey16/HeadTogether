{
  "name": "headtogether",
  "private": true,
  "version": "0.1.0",
  "workspaces": ["apps/web"],
  "scripts": {
    "web:dev": "npm run dev --workspace @headtogether/web",
    "web:build": "npm run build --workspace @headtogether/web",
    "web:lint": "npm run lint --workspace @headtogether/web",
    "web:typecheck": "npm run typecheck --workspace @headtogether/web"
  }
}
